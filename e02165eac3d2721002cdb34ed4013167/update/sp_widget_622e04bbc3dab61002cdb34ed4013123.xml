<?xml version="1.0" encoding="UTF-8"?><record_update table="sp_widget">
    <sp_widget action="INSERT_OR_UPDATE">
        <category>custom</category>
        <client_script><![CDATA[api.controller = function($window, $location) {
  var c = this;
 
  // Set the current page based on the URL to handle the 'active' class
  c.currentPage = $location.search().id || 'dashboard';
 
  c.navigate = function(pageId) {
    // If the target is 'art', navigate to the portal root
    if (pageId === 'art') {
      $window.location.href = '/art';
    } else {
      // Otherwise, navigate to the specific page ID within the portal
      $window.location.href = '/art?id=' + pageId;
    }
  };
};]]></client_script>
        <controller_as>c</controller_as>
        <css>/* ROOT STRUCTURE */
.asset-dashboard-root {
  display: flex;
  min-height: 100vh;
  width: 100%;
  background: linear-gradient(135deg, #1a0b2e 0%, #31135e 100%);
  color: #fff;
  overflow-x: hidden;
}

/* FIXED SIDEBAR (Maintained consistency) */
.side-nav {
  width: 260px;
  background: rgba(0, 0, 0, 0.2);
  backdrop-filter: blur(20px);
  border-right: 1px solid rgba(255, 255, 255, 0.1);
  display: flex;
  flex-direction: column;
  padding: 30px 0;
  position: fixed;
  height: 100vh;
  z-index: 100;
  .nav-header { display: flex; align-items: center; padding: 0 25px; margin-bottom: 50px; 
    .logo-box { background: #7c3aed; padding: 8px 10px; border-radius: 8px; margin-right: 12px; }
    .brand-name { font-weight: 700; font-size: 1.1rem; }
  }
  .nav-links { list-style: none; padding: 0 15px; flex-grow: 1; 
    li { padding: 14px 20px; border-radius: 12px; display: flex; align-items: center; cursor: pointer; color: rgba(255,255,255,0.6); margin-bottom: 8px;
      i { margin-right: 15px; width: 20px; text-align: center; }
      &amp;:hover, &amp;.active { background: rgba(124, 58, 237, 0.2); color: #fff; i { color: #a78bfa; } }
    }
  }
  .nav-footer { padding: 20px; border-top: 1px solid rgba(255,255,255,0.05); 
    .user-pill { display: flex; align-items: center; background: rgba(255,255,255,0.05); padding: 10px; border-radius: 12px;
      .user-init { width: 35px; height: 35px; background: #4c1d95; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-weight: bold; margin-right: 10px; }
    }
  }
  .accent-glow { position: absolute; right: 0; top: 0; width: 2px; height: 100%; background: linear-gradient(to bottom, #7c3aed, #ec4899); }
}

/* MAIN BODY OFFSET */
.main-body {
  flex-grow: 1;
  margin-left: 260px; /* IMPORTANT: Keeps content from touching the side-nav */
  padding: 60px 80px;
  .header-section { margin-bottom: 40px; h1 { font-weight: 700; font-size: 2.5rem; } p { opacity: 0.6; } }
}

/* ANALYTICS UI */
.glass-card {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(15px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 20px;
  padding: 30px;
}

.analytics-highlights {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 25px;
  margin-bottom: 40px;
  .h-label { font-size: 0.75rem; text-transform: uppercase; opacity: 0.5; letter-spacing: 1px; }
  .h-value { font-size: 2.2rem; font-weight: 700; margin: 10px 0; }
  .h-sub { font-size: 0.8rem; font-weight: 600; }
}

.analytics-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 30px; }
.chart-container h3 { margin-bottom: 25px; font-size: 1.2rem; }

/* BAR CHART STYLING */
.bar-row { margin-bottom: 20px;
  .bar-meta { display: flex; justify-content: space-between; margin-bottom: 8px; font-size: 0.9rem; }
  .count-badge { background: rgba(255,255,255,0.1); padding: 2px 8px; border-radius: 4px; font-size: 0.8rem; }
}

.progress-track { background: rgba(255, 255, 255, 0.1); height: 10px; border-radius: 10px; overflow: hidden; }
.progress-fill { background: linear-gradient(90deg, #7c3aed 0%, #ec4899 100%); height: 100%; border-radius: 10px; transition: width 0.8s ease-in-out; }

.line-chart-svg { width: 100%; height: 150px; }
.chart-legend { display: flex; justify-content: space-between; font-size: 0.75rem; opacity: 0.4; }

.text-green { color: #34d399; }
.text-yellow { color: #fbbf24; }
.text-purple { color: #a78bfa; }

</css>
        <data_table>sp_instance</data_table>
        <demo_data/>
        <description/>
        <docs/>
        <field_list/>
        <has_preview>true</has_preview>
        <id>executive_analytics</id>
        <internal>false</internal>
        <link><![CDATA[function link(scope, element, attrs, controller) {  }]]></link>
        <name>Executive Analytics</name>
        <option_schema/>
        <public>false</public>
        <roles/>
        <script><![CDATA[(function() {
    var user = gs.getUser();
    data.user_first_name = user.getFirstName();
    data.user_initials = (user.getFirstName().charAt(0) + user.getLastName().charAt(0)).toUpperCase();
    data.project_name = "IT Asset Management";

    data.stats = {
        total_requests: 0,
        total_cost: 0,
        top_asset: "None"
    };
    data.chartData = [];
    
    var requestTable = 'x_1862743_asset_re_asset_request_table';
    var catalogTable = 'x_1862743_asset_re_asset_catalog_table';

    // 1. NEW LOGIC: Calculate Total Inventory Value (Catalog Table)
    // We iterate through all assets to multiply Quantity * Cost
    var totalValue = 0;
    var grInventory = new GlideRecord(catalogTable);
    grInventory.addActiveQuery();
    grInventory.query();
    while (grInventory.next()) {
        var qty = parseInt(grInventory.getValue('available_quantity')) || 0;
        var cost = parseFloat(grInventory.getValue('unit_cost')) || 0;
        totalValue += (qty * cost);
    }
    data.stats.total_cost = totalValue.toLocaleString('en-GB', {minimumFractionDigits: 2});

    // 2. Get Total Requests count (Request Table)
    var gaReq = new GlideAggregate(requestTable);
    gaReq.addAggregate('COUNT');
    gaReq.setGroup(false);
    gaReq.query();
    if (gaReq.next()) {
        data.stats.total_requests = parseInt(gaReq.getAggregate('COUNT')) || 0;
    }

    // 3. Get Top Requested Asset (Request Table)
    var topAsset = new GlideAggregate(requestTable);
    topAsset.addAggregate('COUNT', 'request_asset');
    topAsset.orderByAggregate('COUNT', 'request_asset');
    topAsset.query();
    if (topAsset.next()) {
        data.stats.top_asset = topAsset.request_asset.getDisplayValue();
    }

    // 4. Prepare Chart Data (Requests by Approval Status)
    var stateCount = new GlideAggregate(requestTable);
    stateCount.addAggregate('COUNT', 'approval_status');
    stateCount.groupBy('approval_status');
    stateCount.query();
    while (stateCount.next()) {
        data.chartData.push({
            label: stateCount.approval_status.getDisplayValue() || 'Pending',
            value: parseInt(stateCount.getAggregate('COUNT', 'approval_status'))
        });
    }

    // 5. Calculate Success Rate Percentage
    var approvedCount = 0;
    data.chartData.forEach(function(item) {
        if(item.label.toLowerCase() == 'approved') approvedCount = item.value;
    });
    data.success_rate = (data.stats.total_requests > 0) ? Math.round((approvedCount / data.stats.total_requests) * 100) : 0;

})();]]></script>
        <servicenow>false</servicenow>
        <sys_class_name>sp_widget</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2026-01-14 06:01:31</sys_created_on>
        <sys_id>622e04bbc3dab61002cdb34ed4013123</sys_id>
        <sys_mod_count>19</sys_mod_count>
        <sys_name>Executive Analytics</sys_name>
        <sys_package display_value="Asset Request and Tracking" source="x_1862743_asset_re">e02165eac3d2721002cdb34ed4013167</sys_package>
        <sys_policy/>
        <sys_scope display_value="Asset Request and Tracking">e02165eac3d2721002cdb34ed4013167</sys_scope>
        <sys_update_name>sp_widget_622e04bbc3dab61002cdb34ed4013123</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2026-01-16 07:00:53</sys_updated_on>
        <template><![CDATA[<div class="asset-dashboard-root">
  
  <aside class="side-nav">
    <div class="nav-header">
      <div class="logo-box"><i class="fa fa-cubes"></i></div>
      <span class="brand-name">{{data.project_name}}</span>
    </div>

    <ul class="nav-links">
      <li ng-click="c.navigate('art')">
        <i class="fa fa-th-large"></i> <span>Dashboard</span>
      </li>
      <li ng-click="c.navigate('browse_and_request_page')">
        <i class="fa fa-plus-circle"></i> <span>Request Asset</span>
      </li>
      <li ng-click="c.navigate('track_my_request_page')">
        <i class="fa fa-search"></i> <span>Track Requests</span>
      </li>
      <li ng-click="c.navigate('my_assigned_inventory_page')">
        <i class="fa fa-laptop"></i> <span>My Assigned Assets</span>
      </li>
      <li ng-click="c.navigate('admin_approval_page')">
        <i class="fa fa-check-square-o"></i> <span>Admin Approval</span>
      </li>
      <li class="active" ng-click="c.navigate('executive_analytics_page')">
        <i class="fa fa-bar-chart"></i> <span>Analytics</span>
      </li>
    </ul>

    <div class="nav-footer">
      <div class="user-pill">
        <div class="user-init">{{data.user_initials}}</div>
        <div class="user-info">
          <div class="u-name">{{data.user_first_name}}</div>
          <div class="u-role">Administrator</div>
        </div>
      </div>
    </div>
    <div class="accent-glow"></div>
  </aside>

  <main class="main-body">
    <div class="header-section">
      <h1>Executive Analytics</h1>
      <p>High-level insights into asset distribution, lifecycle, and costs</p>
    </div>

    <div class="analytics-highlights">
      <div class="glass-card highlight-box">
        <span class="h-label">Total Inventory Value</span>
        <h2 class="h-value">£{{data.stats.total_cost}}</h2>
        <span class="h-sub text-green"><i class="fa fa-arrow-up"></i> Top Asset: {{data.stats.top_asset}}</span>
      </div>
      <div class="glass-card highlight-box">
        <span class="h-label">Total Requests</span>
        <h2 class="h-value">{{data.stats.total_requests}}</h2>
        <span class="h-sub text-yellow">Lifetime submitted requests</span>
      </div>
      <div class="glass-card highlight-box">
        <span class="h-label">Success Rate</span>
        <h2 class="h-value">{{data.success_rate}}%</h2>
        <span class="h-sub text-purple">Approved vs. Submissions</span>
      </div>
    </div>

    <div class="analytics-grid">
      <div class="glass-card chart-container">
        <h3>Request Status Distribution</h3>
        <div class="chart-content">
          <div class="bar-row" ng-repeat="item in data.chartData">
            <div class="bar-meta">
              <span class="bar-label">{{item.label}}</span>
              <span class="count-badge">{{item.value}}</span>
            </div>
            <div class="progress-track">
              <div class="progress-fill" ng-style="{'width': ((item.value / data.stats.total_requests) * 100) + '%'}"></div>
            </div>
          </div>
        </div>
      </div>

      <div class="glass-card chart-container">
        <h3>Request Volume Trend</h3>
        <div class="chart-visual">
          <svg viewBox="0 0 400 150" class="line-chart-svg">
            <path d="M0,120 Q50,110 100,130 T200,80 T300,90 T400,40" fill="none" stroke="#7c3aed" stroke-width="3" />
            <circle cx="100" cy="130" r="4" fill="#ec4899" />
            <circle cx="200" cy="80" r="4" fill="#ec4899" />
            <circle cx="400" cy="40" r="4" fill="#ec4899" />
          </svg>
          <div class="chart-legend">
            <span>Jan</span><span>Feb</span><span>Mar</span><span>Apr</span><span>May</span><span>Jun</span>
          </div>
        </div>
      </div>
    </div>
  </main>
</div>]]></template>
    </sp_widget>
</record_update>
